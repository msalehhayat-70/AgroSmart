<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.user.UserFragment">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsing_toolbar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:contentScrim="?attr/colorPrimary"
            app:layout_scrollFlags="scroll|exitUntilCollapsed">

            <ImageView
                android:id="@+id/userBackgroundImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:background="@color/colorPrimary"
                app:layout_collapseMode="parallax" />

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:layout_collapseMode="pin" />

        </com.google.android.material.appbar.CollapsingToolbarLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- User Profile Image -->
            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/userImageUserFrag"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:layout_marginTop="-70dp"
                android:src="@mipmap/ic_launcher"
                app:civ_border_color="@android:color/white"
                app:civ_border_width="2dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- User Name -->
            <TextView
                android:id="@+id/userNameUserProfileFrag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:textSize="20sp"
                android:textStyle="bold"
                tools:text="User Name"
                app:layout_constraintStart_toEndOf="@+id/userImageUserFrag"
                app:layout_constraintTop_toTopOf="@+id/userImageUserFrag" />

            <!-- User Email -->
            <TextView
                android:id="@+id/userEmailUserProfileFrag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="user@example.com"
                app:layout_constraintStart_toStartOf="@+id/userNameUserProfileFrag"
                app:layout_constraintTop_toBottomOf="@+id/userNameUserProfileFrag" />

            <!-- User City -->
            <TextView
                android:id="@+id/userCityUserProfileFrag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="City: New York"
                app:layout_constraintStart_toStartOf="@+id/userEmailUserProfileFrag"
                app:layout_constraintTop_toBottomOf="@+id/userEmailUserProfileFrag" />

            <!-- About Me TextView -->
            <TextView
                android:id="@+id/aboutValueUserProfileFrag"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                tools:text="About me..."
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/userImageUserFrag" />

            <!-- Editable About Me -->
            <EditText
                android:id="@+id/aboutValueEditUserProfileFrag"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:hint="About me..."
                android:visibility="gone"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/userImageUserFrag" />

            <!-- Editable City -->
            <EditText
                android:id="@+id/cityEditUserProfile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:hint="City"
                android:visibility="gone"
                app:layout_constraintStart_toStartOf="@+id/userCityUserProfileFrag"
                app:layout_constraintTop_toTopOf="@+id/userCityUserProfileFrag" />

            <!-- Upload Progress Bars -->
            <ProgressBar
                android:id="@+id/uploadProgressBarProfile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@+id/userImageUserFrag"
                app:layout_constraintStart_toStartOf="parent"/>

            <ProgressBar
                android:id="@+id/uploadBackProgressProfile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="@+id/userBackgroundImage"
                app:layout_constraintEnd_toEndOf="parent"
                tools:ignore="NotSibling" />

            <!-- Upload Buttons -->
            <ImageView
                android:id="@+id/uploadProfilePictureImage"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_upload"
                android:visibility="gone"
                app:layout_constraintBottom_toBottomOf="@+id/userImageUserFrag"
                app:layout_constraintEnd_toEndOf="@+id/userImageUserFrag" />

            <ImageView
                android:id="@+id/uploadUserBackgroundImage"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_upload"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="@+id/userBackgroundImage"
                app:layout_constraintEnd_toEndOf="@+id/userBackgroundImage"
                tools:ignore="NotSibling" />

            <!-- Edit / Save Buttons -->
            <ImageView
                android:id="@+id/imageEdit"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/ic_edit"
                app:layout_constraintTop_toTopOf="@+id/userImageUserFrag"
                app:layout_constraintEnd_toEndOf="parent"/>

            <ImageView
                android:id="@+id/imageChecked"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/ic_check"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="@+id/userImageUserFrag"
                app:layout_constraintEnd_toEndOf="parent"/>

            <!-- RecyclerView for User Posts -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/userProfilePostsRecycler"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/aboutValueUserProfileFrag" />

            <!-- Optional Posts Count -->
            <TextView
                android:id="@+id/userPostsCountUserProfileFrag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Posts: 0"
                app:layout_constraintTop_toBottomOf="@+id/userCityUserProfileFrag"
                app:layout_constraintStart_toStartOf="@+id/userCityUserProfileFrag" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.core.widget.NestedScrollView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
