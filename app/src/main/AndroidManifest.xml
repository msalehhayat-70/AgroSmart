<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agrosmart">

    <!-- Best practice: Request both FINE and COARSE location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Needed only if your app targets Android 5.0 (API level 21) or higher. -->
    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.agrosmart.view.introscreen.IntroActivity"
            android:exported="false"
            android:theme="@style/AppTheme2" />

        <activity
            android:name="com.example.agrosmart.view.ecommerce.RazorPayActivity"
            android:exported="false" />

        <!--
         This is the main launcher activity.
         It MUST be set to exported="true" to be launched by the home screen.
        -->
        <activity
            android:name="com.example.agrosmart.view.dashboard.DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.agrosmart.view.auth.SignupActivity"
            android:exported="false"
            android:noHistory="true"
            android:theme="@style/AppTheme2" />
        <activity
            android:name="com.example.agrosmart.view.auth.LoginActivity"
            android:exported="false"
            android:noHistory="true"
            android:theme="@style/AppTheme2" />

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="@string/key_id" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>
